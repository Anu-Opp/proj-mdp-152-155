---
- name: Delete Kubernetes Cluster
  hosts: localhost
  tasks:
    - name: Delete Cluster
      shell: |
        export KOPS_STATE_STORE=s3://anu-kops-state-store  # <<< personalize bucket name
        kops delete cluster --name dev.k8s.local --yes
